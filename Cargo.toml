[package]
name = "markdownlector"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "AGPL-3.0-only"
description = "A reading appliance for Markdown in the terminal."
readme = "README.md"

[dependencies]
pulldown-cmark = { version = "0.13", default-features = false }
